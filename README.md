# Dikshya Portfolio

UI/UX Designer portfolio built with pure HTML/CSS/JS, served via Vite + GitHub Pages.

## Quickstart

```bash
pnpm install
pnpm dev      # http://localhost:5173
pnpm build    # dist/
pnpm deploy   # pushes to gh-pages branch
```
